(()=>{const links=[...document.querySelectorAll('.sample-open')],dialog=document.querySelector('.viewer'),image=document.querySelector('#viewer-image'),stage=document.querySelector('.viewer-stage'),zoom=document.querySelector('#viewer-zoom');let index=0,opener;
function resetZoom(){stage.classList.remove('zoomed');zoom.textContent='Zoom in';zoom.setAttribute('aria-pressed','false');stage.scrollTop=stage.scrollLeft=0}
function show(i){index=(i+links.length)%links.length;const a=links[index];image.src=a.href;image.alt=a.querySelector('img').alt;document.querySelector('#viewer-title').textContent=a.dataset.title;document.querySelector('#viewer-kind').textContent=a.dataset.kind;const project=document.querySelector('#viewer-project');project.hidden=!a.dataset.project;if(a.dataset.project)project.href=a.dataset.project;else project.removeAttribute('href');document.querySelector('#viewer-count').textContent=`${index+1} / ${links.length}`;resetZoom()}
links.forEach((a,i)=>a.addEventListener('click',e=>{if(e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;e.preventDefault();opener=a;show(i);dialog.showModal();document.body.classList.add('viewer-open');document.querySelector('#viewer-close').focus()}));document.querySelector('#viewer-close').onclick=()=>dialog.close();dialog.addEventListener('close',()=>{document.body.classList.remove('viewer-open');resetZoom();opener?.focus()});document.querySelector('#viewer-next').onclick=()=>show(index+1);document.querySelector('#viewer-prev').onclick=()=>show(index-1);zoom.onclick=()=>{const on=stage.classList.toggle('zoomed');stage.style.setProperty('--zoom-width',`${Math.max(image.naturalWidth,900)}px`);zoom.textContent=on?'Fit image':'Zoom in';zoom.setAttribute('aria-pressed',String(on));stage.scrollTop=stage.scrollLeft=0};dialog.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){e.preventDefault();show(index+1)}if(e.key==='ArrowLeft'){e.preventDefault();show(index-1)}})})();
